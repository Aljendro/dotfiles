---
- name: Downloading Neovim
  git:
    repo: 'https://github.com/neovim/neovim.git'
    dest: /tmp/neovim
    version: v0.5.1
    depth: 1

- name: Build Neovim
  make:
    chdir: /tmp/neovim
    params:
      CMAKE_BUILD_TYPE: Release

- name: Install Neovim
  become: yes
  make:
    chdir: /tmp/neovim
    target: install

- name: Creating session directory
  file:
    path: ~/.config/nvim/sessions
    state: directory
    mode: '0700'

- name: Install Packages
  shell: neovim +PackerInstall

- name: Compile Packages
  shell: neovim +PackerCompile

- name: Install Lsp Servers
  shell: neovim +LspInstall {{ item }}
  loop:
    - bash
    - clojure
    - css
    - dockerfile
    - html
    - json
    - lua
    - rust
    - typescript
    - vim
    - yaml
