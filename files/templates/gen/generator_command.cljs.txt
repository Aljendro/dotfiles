(ns aljendro.cli.commands.gen.{{name}}
  (:require [clojure.tools.cli :refer [parse-opts]]))

(def ^:private options
  [["-h" "--help" "Show help"]
   ;; Add options here
   ])

(defn run [args]
  (let [{:keys [options errors]} (parse-opts args options)]
    (cond
      (:help options)
      (println "Usage: t gen {{name}} [options]")

      errors
      (do (doseq [e errors] (println e))
          (js/process.exit 1))

      :else
      ;; {{description}}
      nil)))
