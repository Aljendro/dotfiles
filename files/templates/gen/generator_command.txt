import path from 'path';
import { renderAt } from '@aljendro/cli_utilities/handlebars';

const { DOTFILES_DIR } = process.env;

export default {
  command: '{{ name }}',
  describe: '{{ description }}',
  builder: {
    // Add inputs here
  },
  handler: async (argv) => {
    await renderAt(path.join(DOTFILES_DIR, '/some/template/path.txt'), `${process.cwd()}/some/output/path`, argv);
  },
};
